# 2026-08-28


- memory_type: session-derived
  record_format_version: 1.0
  writer: governance_tools.memory_record
  what_changed: PR #36 (feat/spec-qa-pdf-rag) P1 review fix, commit 85304d5: corpus.lock.yaml's per-source included_chapters Phase-1 allowlist was never enforced during PDF ingestion, so any chapter present in a locked/hash-verified PDF became a citable GovernedChunk regardless of Phase-1 authorization. Fixed via parse_included_chapters() + a single _emit()/_chapter_allowed() choke point in chunk_pdf() covering heading/paragraph/table chunks, wired included_chapters through ingest_source_from_corpus_lock(), and added is_official_raw_pdf_source() so load_accepted_chunks() distinguishes PDF-ingestible sources (official_raw + env:// locator) from non-PDF eligible evidence sources (e.g. hub_reference, repo:// locator) instead of conflating the two. test_pdf_ingestion.py grew 14 -> 24 tests including a same-PDF-different-corpus-source-scope regression mirroring the real usb20_fw/usb20_se split. PR body refreshed to reflect 24 tests / 574 passed and a second independent review pass returned PASS (no new P0/P1). Attempted gh pr merge 36 --merge; blocked because head branch is BEHIND base (not a code defect) -- merge not yet completed.
  commit: 85304d5
  commit_hash: 85304d5
  session_id: spec-qa-pdf-rag-pr36-p1-fix-20260828
  memory_binding: bound
  test_evidence: pytest tests/ -q -> 574 passed, 2 skipped (pre-existing, unrelated); pytest tests/gv100h/test_pdf_ingestion.py --collect-only -> 24 collected (up from 14); get_errors on pdf_ingestion.py and test_pdf_ingestion.py -> no errors; gh pr view 36 --json headRefOid == 85304d5; gh pr checks 36 -> all checks successful. NOT CLAIMED: merge completed (gh pr merge 36 --merge failed: head branch not up to date with base, requires --auto/--admin or a branch update, not yet performed).
  next_step: Bring feat/spec-qa-pdf-rag up to date with origin/main (merge or rebase), re-verify CI green and mergeStateStatus, then merge PR #36 (gh pr merge 36 --merge) once explicitly re-confirmed by the user; next Machine A slice after merge is a Real Corpus Smoke Test against the actual locked USB 2.0/USB 3.2/SuperSpeed Hub LVS PDFs (per reviewer's follow-up), and included_scope machine-enforcement remains open P2 governance debt before live retrieval integration.
  plan_reconciliation: deferred:scope-split-next-slice
  record_identity: e2f797cf4fc5c71b5e2c3495f93d5d36926b3e489fe5e46986ec2b9c4e74270b
